from github, create new project

git init
git branch -M main
git remote add origin https://github.com/tiennguyen5118/<repo-name>.git
git config user.email "tiennguyen5118@gmail.com"
git config user.name "Tien Nguyen"
git push -u origin main